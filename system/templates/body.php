<?php defined('ALLOW_INCLUDE') or die('No direct access allowed.');              

?>

        <div class="wrap" id="maincontent">
            <?php
                if(isset($body_content)) echo $body_content;
            ?>
        </div>